from django.contrib import admin
from django.urls import path
from . import views

urlpatterns = [
    # path('userlist/', views.UserList.as_view()),
    # path('usercreate/', views.UserCreate.as_view()),
    # path('userdelete/<int:id>', views.UserDestroy.as_view()),
    # path('clientlist/', views.ClientList.as_view()),
    # path('clientcreate/', views.ClientCreate.as_view()),
    # path('clientdelete/<int:client_id>', views.ClientDestroy.as_view()),
]